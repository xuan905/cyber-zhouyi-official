# ClawHub metadata 更新紀錄

目前已進入 `zhouyi-site-skill` 的 ClawHub Settings → Catalog metadata。初始狀態為 1 個分類、0 個 Topics。

嘗試逐一輸入 `易經`、`六爻`、`decision-support` 並按 Enter 時，ClawHub 畫面每次顯示 1 個 topic 且只保留最後一次輸入的 token；因此改以不清空既有 token 的方式補入缺少項目。

最終在儲存前確認 Topics 為 `decision-support`、`易經`、`六爻`，ClawHub 回報 `Catalog metadata updated.`。重新載入 Settings 後仍顯示 `1 category · 3 topics` 及三個 token；公開 Skill 頁亦更新為 `Last updated: just now`，Current version 維持 `v0.1.1`。
